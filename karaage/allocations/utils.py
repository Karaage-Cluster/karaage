def create_allocation_pool_if_required(new_allocation):
    """
    Checks to see if there is an allocation pool for the new allocation to be
    added to.

    Adds the allocaiton pool if it doesn't exist.
    """

    
